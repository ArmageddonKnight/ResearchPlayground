name: Action1
description: Action1
inputs:
  some-input:
    description: Some input.
runs:
  using: composite
  steps:
    - shell: bash
      if: ${{ inputs.some-input == '' }}
      run: echo "123"
    - shell: bash
      run: |
        echo ${{ github.sha }}
        echo ${{ secrets.GCP_CREDENTIALS_JSON }}
    - shell: python
      run: |
        print("${{ inputs.some-input }}")
    - shell: python
      run: |
        print(${{ inputs.some-input }})
